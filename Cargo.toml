[package]
name = "branchcut"
version = "0.1.0"
edition = "2024"
description = "A zero-dependency filesystem query engine"
license = "MIT"

[dependencies]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
